# Footprint 项目详细功能总结 (Project Summary)

Footprint 是一款以“地理探索、回忆沉淀与数字艺术再创造”为核心理念的移动端应用。它深度结合了高精度地图引擎、局部混合跨平台开发架构 (Native Kotlin/Jetpack Compose 1.7 + Flutter) 和底层渲染极速引擎，构建出了极具沉浸感的记录体验和硬核的资产管理闭环。

以下为本项目截至目前已实现的详尽功能清单：

## 1. 核心业务逻辑与数据管理服务

- **个性化位置与轨迹记录模型**
  - **精确定位**：深度集成高德地图 (AMap) SDK，支持经纬度坐标精确到小数点后三位，记录涵盖位置标题、精准发生时间、文字日记以及图文影像的空间数据。
  - **LocationTrackingService 高级后台追踪器**：基于 Android 原生的混合定位服务（含前台 Service），基于设备 GPS 芯片全自动平滑勾勒真实移动行迹。
  - **极限电池压榨逻辑**：阻断无意义的后台轮询，仅在全天候记录模式开启或用户处于地图主界面时执行空间拉取，耗电极低。
  - **轨迹航向连续性防断裂校准**：处理弱 GPS 信号下的坐标异常偏差，保障绘制出来的轨迹线不反弹折回。

- **安全的离线数据闭环 (Local Data & Sync)**
  - **Room 架构 SQLite 存储**：不依赖任何外部云服务（除非主动分享/调用AI），实现全部日记、地图轨迹的本地强一致性持久化保存（适配至 schema v9）。
  - **防破坏数字灾备 (JSON & ZIP 本地系统)**：
    - 支持完整数据库结构的 JSON 解析导入/导出。
    - **ZIP 打包机制**：将 SQLite 的文本信息与其配套引用的数GB高清原图进行二进制归档压缩，对接 Android Storage Access Framework 安全恢复。

- **全局应用配置与身份鉴权**
  - **数字代号系统**：每位探索者均可在软件内设定一个代表自我的隐匿代号以及形象，替代传统的手机号账户。
  - **动态安全密钥 (API Key injection)**：具备界面层级的动态输入并本地固化高德密钥功能。

## 2. 独创沉浸式地图生态引擎

- **多风格环境底图系统**
  - 自适应底图瓦片引擎支持：“标准”、“暗夜”、“卫星观测”、“虚空”四种无缝切换。
  - Android 暗色模式 / 亮色系统自动适配跟随：地图环境自动追随手机昼夜节律。

- **🌟 纯血高性能迷雾探索 (Fog of War) —— 终极渲染黑科技**
  - 基于高阶程序化分形噪声 (Fractal Noise) & AGSL 碎云物理着色模拟真实环境下的云遮雾绕。
  - **动态视野突破与边缘柔化侵蚀**：记录过的历史点和实时前进的实体轨迹线如破冰般排斥、溶解迷雾，实现吞噬感。
  - **Web 墨卡托矩阵 (C++ Native Matrix) 极限优化**：通过离线计算 Web 墨卡托坐标和局部投影的三点矩阵放射技术，一帧内的几何计算效率从巨量堆叠的深度 Java/JNI 函数直接转换为 O(1) 块内存调用，几十万地理轨迹同屏滑动拖拽稳锁 60fps 不发热不卡顿。

- **时空胶囊 (Time Capsules) 的埋藏与触发**
  - 允许在现实世界某个真实的地理切片处“埋放”一个足迹/回忆胶囊。
  - **严密的时空间维度解锁**：用户只能在未来的特定时间抵达或者当手机地理位置重新接近（如100米范围）该埋藏点才可通过感应“挖出”胶囊，强化时空穿梭感。

## 3. UI 交互革命、动画引擎与多端架构

- **多端流式内嵌架构 (Hybrid Architecture)**
  - 采用 Android (Kotlin / Compose) 端作为绝对底层逻辑接管性能核心模块（渲染、耗能、DB）。
  - 采用 Flutter Module 无缝嵌入作为部分多媒体图层及动画富交互 UI 的容器。通道之间通过 BinaryMessenger (MethodChannel) 实现几乎零延迟的毫秒通信桥梁（例：定位相机跳转、迷雾坐标更新）。

- **系统沉浸感与主题色跟随**
  - **无死角暗色 / 亮色模式 (Dark/Light)**：从状态栏、浮动按钮、弹窗的毛玻璃遮罩到字色全部独立受系统主题色和环境接管（Material Design 3 标准），支持随意切换不闪屏。
  - **主题重定制色散引擎**：内置主题包括了（深空蓝/赛博粉/Neon绿/流金 等）颜色同步到地图足迹轨迹点上解耦实现。

- **全局弹性环境碰撞逻辑 (Physical Feedback Motion)**
  - **防生硬物理弹跳 (Bouncy Click)**：移除基础的安卓涟漪水纹，赋予屏幕所有图标（侧滑、悬浮加号等）点击重压收缩后弹性膨胀动画。
  - 列表中结合 `snapAnimationSpec` 控制滑动阻尼。

- **玻璃拟态与液态视觉 (Glassmorphism & Liquids)**
  - 基于高级 AGSL，呈现真实的“光学校正”“菲涅尔边缘泛光”与 “色散液态 (Chromatic Aberration) 重构球体”界面观感，完美降维适配 Android 12 的模糊后备方案。

## 4. “艺术工坊”(Zigapixel Studio) 与海报导出演算

- **全息天气与情感能量胶囊流体球**
  - 研发出高达 29 种的世界气象（WMO）流体动态气象胶囊（晴岚至极寒），以动态色散渲染着色器形式封装。
  - 心情值、体力能量块通过内置的心情调色盘球体和几何雷达扫描粒子进行交互级显示。

- **极清像素无损海报分块引擎 (Rust Rendering Core + Zigapixel)**
  - 调用底层 Rust(`libfootprint_renderer.so`) 二元库直接劫持绘制流水线，彻底打破 Java Bitmap 显存上限，能在极短时间输出可印刷级别的 6K 全图海报。

- **重装机甲、拍立得、复古 A24 排版面板生成器**
  - “工坊”不仅只导出图片，更能为导出的足迹提供高度专业化的外包装：例如赋予装甲战术 HUD 数据面板表面拉丝、电影复古质感，并且副标题日期总里程皆可自适应。

- **中国书法大字库赋能嵌入**
  - 在导出界面的文字层引入三大硬核毛笔字体：龙仓草书 (LongCang)、志莽行书 (ZhiMangXing)、马善政毛笔 (MaShanZheng)，极大提高成品中华文化艺术张力。
  
- **全息 HTML 无限放大 3D 沙盘衍生法**
  - 支持借由 MapLibre GL 解析算法通过道格拉斯-普克无感压缩冗长 GPS 点序列，将旅途打包输出为一个无需外部容器即可跨设备任意放大且流畅浏览的静态沙盘 HTML 聚合文件。

## 5. “时光仪表盘”数据聚合洞察面板 (Dashboard Analytics)

- **Github 代码推拽级别心情热力块墙 (Heatmap)**
  - 首页整合了一张横跨全年 365 天的时间坐标热力砖块图，并将其染以每日主体的心情色斑，一瞥即知岁月变化。

- **抽屉式无限下钻折叠**
  - 将海量足迹的年度瀑布流（TimeLine）以极有逻辑思维的“月”份包裹（Accordion 折叠逻辑），兼得快速浏览概览结构与防内存坍塌。

- **史诗级成就头衔授予计算 (Titles & Badges)**
  - 提供细粒度的数据分析统计底层框架，并赋予游戏化的正向反馈激励：从青铜新手到传奇旅行家的身份随着每一次脚印积累自动点亮。

- **足迹修改与AI自动化书写助理**
  - 全局支持对任意时间地点的脚印属性调用编辑界面进行增删改。
  - 支持接入前沿端侧或者大语言模型 AI 的 `Story Writer Generative AI Dialog` 进行提示词智能扩写，免去繁杂的手动键盘游记记录。

---

> **本摘要文件 (PROJECT_SUMMARY.md) 定位：**
> 作为 Footprint 工程自立项以来的全量全景视觉逻辑的字典目录，代表了该客户端目前代码仓库的开发复杂度横断面。该文件受工程架构版本维护者管理。
